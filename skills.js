// function to covert Celsius
// to Fahrenheit
function convertCelsiusToFahrenheit(celsius) {
    return (celsius * 9/5) + 32;
}

// Driver code
let celsius = 60;
console.log(celsius + "°C is " + convertCelsiusToFahrenheit(celsius) + "°F");